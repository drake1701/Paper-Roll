<?php
/**
 * @package		PaperRoll
 * @author		Dennis Rogers
 * @address		www.drogers.net
 */
?>

Entries: <br />
<dl>
    <?php foreach ($this->entries as $entry): ?>
    <dt><a href="<?php echo $entry->getUrl()?>"><?php echo $this->escape($entry->getTitle()) ?></a></dt>
    <dd><?php echo $this->escape($entry->getContent()) ?></dd>
    <?php endforeach ?>
</dl>